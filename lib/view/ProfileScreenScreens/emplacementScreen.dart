import 'package:flutter/material.dart';

class EmplacementScreen extends StatelessWidget {
  const EmplacementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Emplacement',
      ),
    );
  }
}
